{
  "name": "biddesk",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent bid response system for small SLED contractors. v1: SHREDDER + GATEKEEPER.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply biddesk --local",
    "migrate:remote": "wrangler d1 migrations apply biddesk --remote",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260101.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
